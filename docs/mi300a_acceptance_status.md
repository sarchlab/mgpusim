# MI300A Acceptance Status

This note consolidates the current MI300A workflow contract, retained stale completed/failure partial artifacts-only finishability evidence, the retained non-terminal/provisional stale run snapshot, later fresh maintained run attempts including the current-main terminal failure with final artifacts, the historical accuracy-report provenance, a final human decision packet, and the remaining caveats. It is a status summary only: it does not claim a clean benchmark run, clean run, complete terminal finishability, final completion, acceptance-complete status, or acceptance of caveated accuracy areas.

## Maintained workflow contract

The maintained MI300A benchmark entry point is `.github/workflows/benchmark.yml` (`MI300A Benchmark`). Its regular path is:

```text
Validation -> Tier 1 -> Tier-1 summary -> Tier 2 -> Summary
```

Maintained contract values:

| Item | Value |
| --- | --- |
| Dispatch inputs | `workflow_dispatch` with only optional `ref`; no mode selector |
| Plan source | `benchmark-comparison/mi300a_problem_size_discovery_plan.json` |
| Regular matrix size | 82 benchmark rows / 1416 plan entries |
| Tier split | Tier 1: 19 rows / 308 entries; Tier 2: 63 rows / 1108 entries |
| Per-simulator invocation cap | `timeout_sec: 7200` seconds |
| Benchmark-tier job cap | `timeout-minutes: 360` / 21600 seconds |
| Benchmark-tier parallelism | `strategy.max-parallel: 14` |

The checked-in finishable manifest and generated finishable Tier views remain audit references; they do not prune the maintained regular runtime matrix.

A separate workflow-hygiene note tracks the stale deleted workflow API record for `.github/workflows/benchmark-tier-runner.yml`: workflow id `260768869` was disabled manually on 2026-05-13, the file is absent from tracked `main`, and the retained API record is benign only while no new runs appear.

## Retained stale finishability evidence

Run `25710089668` is retained stale terminal, completed/failure partial artifacts-only evidence for the maintained regular workflow. It is not completed/success current-main evidence.

| Item | Value |
| --- | --- |
| Run URL | https://github.com/sarchlab/mgpusim-dev/actions/runs/25710089668 |
| Head | `main` / `6fdbbd1882f6a36c5e0846b9209a9a19c258b486` |
| Workflow/event | `MI300A Benchmark` / `workflow_dispatch` |
| Status/conclusion | `completed` / `failure` |
| Evidence files | `benchmark-comparison/selected-run-25710089668/mi300a_regular_finishability_evidence.{json,md}` and `benchmark-comparison/provenance/mi300a_regular_benchmark_run_25710089668_*` |

Artifacts-only finishability summary across the 1416-entry regular plan:

| Status | Count |
| --- | ---: |
| pass | 623 |
| no-result | 793 |
| fail | 0 |
| timeout | 0 |
| cancelled | 0 |

`pass` means a matching per-benchmark result CSV row or checked-in merged simulator timing row was emitted for that problem size. `no-result` means no matching per-size artifact row was present. Missing rows are not relabeled as timeout or crash from job-level failure alone. The run is therefore partial finishability evidence, not a clean pass, not clean benchmark completion, and not proof that all 1416 plan entries finish under the terminal CI run. A successful final `Summary` job in a completed/failure run means report/artifact upload completed; it does not turn partial or failing MI300A Benchmark CI into clean completion evidence.

## Retained non-terminal run snapshot

Run `25841274346` is retained only as the checked-in non-terminal/provisional provenance snapshot from a stale maintained `main` run. The checked-in package stays non-terminal/provisional unless a separate terminal package is committed and validated.

| Item | Value |
| --- | --- |
| Run URL | https://github.com/sarchlab/mgpusim-dev/actions/runs/25841274346 |
| Head | `main` / `e0ef96c57e042c636e2b94db8f294ff8a574bb78` |
| Workflow/event | `MI300A Benchmark` / `workflow_dispatch` |
| Status/conclusion at collection | `in_progress` / `` |
| Collected at | `2026-05-14T10:05:17Z` |
| Evidence files | `benchmark-comparison/selected-run-25841274346/` |

This snapshot is **not terminal evidence**. At collection time the run had `18` completed jobs and `2` in-progress jobs, with job conclusion counts `failure=15`, `success=3`, and `empty=2`. Its artifacts are provisional and may increase until the run reaches `completed`. A later read-only Actions recheck at `2026-05-14T20:28Z` found no newer `main` `workflow_dispatch` `benchmark.yml` run above `25841274346`; live `main` was `404c8f2e09d6b59e7b15a84425437a886a5e2f16`, so run `25841274346` was stale relative to the then-current `main` tip and remained `in_progress` with `conclusion=null`, 84 jobs, 71 completed, 13 in progress, and no final `benchmark-comparison`, `comparison-detailed`, `validation-report`, `accuracy-report`, or final `Summary` job evidence. It does not claim a clean pass, a no-result/failure/cancelled terminal outcome, clean benchmark completion, or complete finishability evidence. Do not derive or claim terminal finishability from this checked-in package; keep it non-terminal/provisional unless a separate terminal package is committed and validated, and re-run the provenance collector after the run reaches `completed` before deriving or claiming terminal finishability evidence.

## Fresh maintained run attempts

After a later `main` advance, the maintained current-main recheck found `origin/main` at `59be524bb40eadfd8b7a44feb6c7e7dab699232b`, no active maintained run at that SHA, and no completed/success current-main run with final Summary and required final artifacts. Because no current-main active run blocked dispatch, exactly one fresh maintained `.github/workflows/benchmark.yml` / `MI300A Benchmark` `workflow_dispatch` launch was made with `ref=main`: #435 / `25962281031`, from `main` / `59be524bb40eadfd8b7a44feb6c7e7dab699232b`, created at `2026-05-16T12:45:56Z`, initially `status=queued` and `conclusion=null`. No artifacts were downloaded at dispatch time, and the launch did not create clean terminal evidence.

The latest read-only adjudication boundary records `origin/main` and remote `main` at `f42286c5e9de738f94b80733ebb0f0cea6123210`. The latest maintained `main` `workflow_dispatch` run for `.github/workflows/benchmark.yml` / `MI300A Benchmark` is #440 / `26213004542`, created at `2026-05-21T07:51:41Z`, updated at `2026-05-22T06:02:19Z`, from `main` / `f42286c5e9de738f94b80733ebb0f0cea6123210`; a latest-run query lists #440 above older same-head run #439, older stale run #438, and older maintained runs. Run #440 matches current `origin/main` and is terminal with top-level `status=completed` and `conclusion=failure`, not completed/success clean evidence. It has 85 completed jobs total; job conclusions are `success=5`, `failure=76`, and `cancelled=4`. The final `Summary: merge, compare, and report` job `77325134348` completed with `success` from `2026-05-22T06:01:53Z` to `2026-05-22T06:02:18Z`. The artifact API lists 87 artifacts, including the required final artifacts `benchmark-comparison` (artifact `7153796957`, 36612 bytes), `comparison-detailed` (`7153797086`, 18209 bytes), `validation-report` (`7153797501`, 11128008 bytes), and `accuracy-report` (`7153797745`, 3713274 bytes). The final Summary log records `complete_regular_evidence=false`, `partial_diagnostic_regular_evidence`, upstream `benchmark-tier1` and `benchmark-tier2` failures, `sim_results_ci.csv` with 632/1416 data rows, and `regression_ci.csv` with 517/1416 data rows. Gate result: current-main terminal failure with final artifacts and successful final Summary upload, but not completed/success and not complete regular evidence; therefore no clean-completion claim, no merge/report regeneration from downloaded artifacts, and no terminal finishability acceptance is derived. Run #440 is the latest current-main terminal failed final-artifact evidence only, not clean terminal evidence, and it does not resolve #2894 or #2893 absent a later completed/success current-main run with `complete_regular_evidence` or explicit real-human acceptance/closure.

A same-head maintained run at the current `main` boundary, #439 / `26146663871`, is now older/stale relative to #440 / `26213004542` even though it used the same head `main` / `f42286c5e9de738f94b80733ebb0f0cea6123210`. Run #439 was created at `2026-05-20T06:56:36Z`, updated at `2026-05-21T04:05:11Z`, and is terminal with top-level `status=completed` and `conclusion=failure`, not completed/success clean evidence. Its final `Summary: merge, compare, and report` job `77102101849` completed with `success`, and the required final artifacts `benchmark-comparison`, `comparison-detailed`, `validation-report`, and `accuracy-report` are present. The final Summary log records `complete_regular_evidence=false`, upstream `benchmark-tier1` and `benchmark-tier2` failures, `sim_results_ci.csv` with 629/1416 data rows, and `regression_ci.csv` with 511/1416 data rows. Gate result: older same-head terminal failure with final artifacts and successful final Summary upload, but not completed/success and not complete regular evidence; therefore no clean-completion claim, no merge/report regeneration from downloaded artifacts, and no terminal finishability acceptance is derived. Run #439 is preserved only as older completed/failure final-artifact evidence stale relative to #440; it is not current latest evidence, not clean terminal evidence, and it does not resolve #2894 or #2893 absent a later completed/success current-main run with `complete_regular_evidence` or explicit real-human acceptance/closure.

An older read-only adjudication boundary recorded `origin/main` and remote `main` at `c12a2e12f3109d036c95be4e4171ac06a87ff74b`. The latest maintained `main` `workflow_dispatch` run for `.github/workflows/benchmark.yml` / `MI300A Benchmark` was then #438 / `26083372148`, created at `2026-05-19T07:40:01Z`, updated at `2026-05-20T05:32:39Z`, from `main` / `c12a2e12f3109d036c95be4e4171ac06a87ff74b`; a latest-run query listed #438 above stale run #437 and older maintained runs. Run #438 matched that then-current `origin/main` and was terminal with top-level `status=completed` and `conclusion=failure`, not completed/success clean evidence. It had 85 completed jobs total; job conclusions were `success=5`, `failure=77`, and `cancelled=3`. The final `Summary: merge, compare, and report` job completed with `success` from `2026-05-20T05:32:13Z` to `2026-05-20T05:32:38Z`. The artifact API listed 87 artifacts, including the required final artifacts `benchmark-comparison`, `comparison-detailed`, `validation-report`, and `accuracy-report`. Gate result: then-current-main terminal failure with final artifacts and successful final Summary upload, but not completed/success and no completed/success `complete_regular_evidence`; therefore no clean-completion claim, no merge/report regeneration from downloaded artifacts, and no terminal finishability acceptance is derived. Run #438 is now older/stale relative to #439 and #440 / `f42286c5e9de738f94b80733ebb0f0cea6123210`; it is not current latest evidence, not clean terminal evidence, and it does not resolve #2894 or #2893 absent a later completed/success current-main run with `complete_regular_evidence` or explicit real-human acceptance/closure.

An earlier read-only adjudication at `2026-05-19T06:27Z` found `origin/main` and remote `main` at `dc453d6c789e180009c5f4bce9d574f77c0bb53e`. The latest maintained `main` `workflow_dispatch` run for `.github/workflows/benchmark.yml` / `MI300A Benchmark` was then #437 / `25993763757`, created at `2026-05-17T14:37:16Z` from `main` / `dc453d6c789e180009c5f4bce9d574f77c0bb53e`; a latest-run query listed #437 above stale run #436 and older maintained runs. Run #437 matched that then-current `origin/main` and was terminal with top-level `status=completed` and `conclusion=failure`, not completed/success clean evidence. It had 85 completed jobs total; job conclusions were `success=5`, `failure=78`, and `cancelled=2`. Both `Tier-1 summary` (job `76451311745`) and final `Summary: merge, compare, and report` (job `76668743631`) completed with `success`; the final Summary job ran from `2026-05-19T04:39:42Z` to `2026-05-19T04:40:07Z`. The artifact API listed 86 artifacts, including required final artifacts `benchmark-comparison` (artifact `7074999643`, 35950 bytes), `comparison-detailed` (`7074999759`, 18006 bytes), `validation-report` (`7075000138`, 11125839 bytes), and `accuracy-report` (`7075000368`, 3660690 bytes). The final Summary log still recorded `complete_regular_evidence=false`, `partial_diagnostic_regular_evidence`, upstream `benchmark-tier1` and `benchmark-tier2` failures, `sim_results_ci.csv` with 618/1416 data rows, and `regression_ci.csv` with 500/1416 data rows. Gate result: then-current-main terminal failure with final artifacts and successful final Summary upload, but not completed/success and not complete regular evidence, so no clean-completion claim, no merge/report regeneration from downloaded artifacts, and no terminal finishability acceptance is derived. Run #437 is now stale relative to #438 / `c12a2e12f3109d036c95be4e4171ac06a87ff74b` and is preserved only as older completed/failure final-artifact evidence; it is not current latest evidence, not clean terminal evidence, and it does not resolve #2894 or #2893 absent a later completed/success current-main run with `complete_regular_evidence` or explicit real-human acceptance/closure.

An earlier read-only adjudication at `2026-05-17T13:29Z` found `origin/main` and remote `main` at `b78207bc1210e0ed2aa6f9b6c9f59a4511b92f35`. The latest maintained `main` `workflow_dispatch` run for `.github/workflows/benchmark.yml` / `MI300A Benchmark` was then #436 / `25974165191`, created at `2026-05-16T22:05:34Z` from `main` / `b78207bc1210e0ed2aa6f9b6c9f59a4511b92f35`; a latest-run query listed #436 above #435 and older maintained runs. Run #436 matched that earlier `origin/main`, but it was still active and non-terminal with top-level `status=in_progress` and `conclusion=null`. It had 20 jobs total: `17` completed and `3` in progress; job conclusions were `success=3`, `failure=12`, `cancelled=2`, and `empty=3`. The still-active Tier 1 jobs were `triad`, `branch_div_50pct`, and `reduction`. No final Summary/summary/compare/report-like job was present. Seventeen available artifacts existed (`validation-summary` plus partial `benchmark-results-*` artifacts), while the required final artifacts remained absent: `benchmark-comparison`, `comparison-detailed`, `validation-report`, and `accuracy-report`. Gate result: then-current-main but active/non-terminal, already failed/cancelled partial, and missing final Summary/report artifacts, so no artifact download, collector, merge, report regeneration, or terminal finishability derivation was run. Run #436 is now stale relative to #438, #439, and #440 / `f42286c5e9de738f94b80733ebb0f0cea6123210` and must not be reused as clean evidence; this package carries only its earlier provisional snapshot boundary and does not resolve #2894 or #2893 absent a later completed/success current-main run with final Summary and required final artifacts or explicit real-human acceptance/closure.

The previous read-only adjudication at `2026-05-16T20:48Z` found the latest maintained `main` `workflow_dispatch` run for `.github/workflows/benchmark.yml` / `MI300A Benchmark` was still #435 / `25962281031`; a latest-run query still listed #435 above all older maintained runs. Run #435's head stayed `main` / `59be524bb40eadfd8b7a44feb6c7e7dab699232b`, but current `origin/main` and remote `main` were `d46ecbf9ed3bd631118a591629d1a977f5d3fff6`, so the run head was stale: an ancestor of current `main` and `4` commits behind. Run #435 was also non-terminal with `status=queued` and `conclusion=null`. It had 20 jobs total: `11` completed, `4` in progress, and `5` queued; job conclusions were `success=3`, `failure=8`, and `empty=9`. No Summary/summary/compare/report-like job was present. Eleven available artifacts existed (`validation-summary` plus `benchmark-results-*` artifacts), while the required final artifacts remained absent: `benchmark-comparison`, `comparison-detailed`, `validation-report`, and `accuracy-report`. Gate result: stale, non-terminal, and missing final Summary/report artifacts, so no artifact download, collector, merge, report regeneration, or terminal finishability derivation was run. Run #435 is queued/provisional stale evidence only, not clean terminal evidence, and it does not resolve #2894 or #2893 absent a later completed/success current-main run with final Summary and required final artifacts or explicit real-human acceptance/closure.

Earlier, after the stale-run wording fix, the maintained current-main recheck found `origin/main` at `0e47eda0a98049723b4a25ac8878a414d29e0e20`, no active run at that SHA, and no completed/success current-SHA run with final Summary and required final artifacts. It records exactly one agent-attributable maintained `.github/workflows/benchmark.yml` / `MI300A Benchmark` `workflow_dispatch` launch at that then-current SHA: #433 / `25946016596`, from `main` / `0e47eda0a98049723b4a25ac8878a414d29e0e20` at `2026-05-15T23:20:04Z`.

The same then-current SHA also has an observed additional maintained `.github/workflows/benchmark.yml` / `MI300A Benchmark` `workflow_dispatch` run, #434 / `25946106740`, with unknown/external provenance unless later evidence proves otherwise. GitHub metadata `actor`/`triggering_actor` value `syifan` is not agent provenance and is not enough to attribute #434 to this package. Therefore the then-current-SHA evidence is not an at-most-one/current-run story while #434 exists.

The previous delayed read-only monitor at `2026-05-16T10:20:57Z` found two then-current-SHA maintained `main` `workflow_dispatch` runs, #434 / `25946106740` and #433 / `25946016596`; both were attempt `1`, event `workflow_dispatch`, headed at `main` / `0e47eda0a98049723b4a25ac8878a414d29e0e20`, and still `conclusion=null`. Run #434 / `25946106740` was top-level `status=in_progress`, had 20 jobs with 17 completed and 3 in progress, and job conclusions were `success=3`, `failure=13`, `cancelled=1`, and `null=3`; final Summary job count was `0`; artifacts totaled `17` (`validation-summary` plus partial benchmark-results artifacts), and all required final artifacts remained absent: `benchmark-comparison`, `comparison-detailed`, `validation-report`, and `accuracy-report`. Run #434 was already failed-partial with 13 failed Tier 1 jobs and `global_bw_copy` cancelled, while `branch_div_50pct`, `triad`, and `reduction` remained in progress. Run #433 / `25946016596` was top-level `status=queued`, had 84 jobs with 21 completed, 7 in progress, and 56 queued; job conclusions were `success=4`, `failure=16`, `cancelled=1`, and `null=63`. It had `Tier-1 summary` completed/success, but not a final workflow Summary because Tier 2 jobs remained active/queued. Artifacts totaled `20` partial benchmark/validation-summary artifacts, and all required final artifacts remained absent. No artifacts were downloaded, no terminal finishability/provenance was derived, and no dispatch/rerun/cancel was performed during the delayed monitor. Artifact collection/validation is not needed or authorized for these non-terminal/failing partial states. These runs are stale relative to later `main` / `59be524bb40eadfd8b7a44feb6c7e7dab699232b` and remain non-terminal/failed-partial/provisional evidence only, not clean terminal evidence; they do not resolve #2894 or #2893 absent a later completed/success current-main run with final Summary/report artifacts or explicit real-human acceptance/closure.

A prior maintained `main` `workflow_dispatch` run, `25910086960`, was started from `main` / `a3c64f59db9e62760468d6043b0c1cf7ab6b32ee` after no completed/success run at that then-current main SHA with final Summary and required final artifacts was found. A later read-only Actions check at `2026-05-16T19:21Z` found run `25910086960` stale relative to current `main` / `59be524bb40eadfd8b7a44feb6c7e7dab699232b` and terminal `completed`/`failure`, not completed/success current-main evidence. It remained on `.github/workflows/benchmark.yml` / `MI300A Benchmark`, event `workflow_dispatch`, from `main` / `a3c64f59db9e62760468d6043b0c1cf7ab6b32ee`, and had 85 completed jobs with conclusion counts `success=5`, `failure=76`, and `cancelled=4`. Both `Tier-1 summary` and final `Summary: merge, compare, and report` completed successfully, and 87 artifacts were available including final `benchmark-comparison`, `comparison-detailed`, `validation-report`, and `accuracy-report` artifacts. The successful final Summary and final artifacts only show report/artifact upload for a stale failed run; they do not make the run a clean pass, clean benchmark completion, completed/success current-main evidence, or complete terminal finishability evidence. No artifacts were downloaded, no terminal finishability/provenance was derived, and no dispatch/rerun/cancel was performed during that check. Run `25910086960` therefore remains stale completed/failure final-artifact evidence only, not clean terminal evidence, and it does not resolve #2894 or #2893 absent a later completed/success current-main run with final Summary/report artifacts or explicit real-human acceptance/closure.

An earlier maintained `main` `workflow_dispatch` run, `25886484910`, was started from `main` / `77b08d4ceb2d55e4f02e31570b349d2857b497e3` after the final human decision packet. Later read-only Actions rechecks found it stale relative to the current `main` tip and terminal `completed`/`failure`, not completed/success current-main evidence. Run `25886484910` had 85 completed jobs with conclusion counts `success=5`, `failure=76`, and `cancelled=4`; both `Tier-1 summary` and final `Summary: merge, compare, and report` completed successfully, and 86 artifacts were available including final `benchmark-comparison`, `comparison-detailed`, `validation-report`, and `accuracy-report` artifacts. The successful final Summary and final artifacts only show report/artifact upload for a stale failed run; they do not make the run a clean pass, clean benchmark completion, completed/success current-main evidence, or complete terminal finishability evidence. No artifacts were downloaded, no terminal finishability/provenance was derived, and no dispatch/rerun/cancel was performed during those follow-up rechecks. This earlier fresh run therefore remains stale completed/failure final-artifact evidence only, does not resolve the clean terminal evidence blocker, and does not change the completed/failure partial-artifacts-only classification for run `25710089668`.

The follow-up metadata recheck also confirmed the retained final-artifact facts for older maintained runs `25841274346` and `25710089668`; both are terminal failures, not clean completion. Run `25841274346` is `completed`/`failure` with 85 completed jobs (`success=5`, `failure=77`, `cancelled=3`) and 86 artifacts; its final-artifact coverage is `partial_diagnostic_regular_evidence`, `Complete regular evidence: false`, with `sim_results_ci.csv` covering 619/1416 rows and `regression_ci.csv` covering 502/1416 rows. Run `25710089668` is `completed`/`failure` with 85 completed jobs (`success=5`, `failure=76`, `cancelled=4`) and 87 artifacts; its final-artifact coverage is also `partial_diagnostic_regular_evidence`, `Complete regular evidence: false`, with `sim_results_ci.csv` covering 632/1416 rows and `regression_ci.csv` covering 515/1416 rows. For both terminal failure runs, the validation report provenance still uses selected-run `25619929396`, and the finishable-size summary points to old non-terminal discovery run `24959959195`; neither run is promoted to clean completion or complete terminal per-size finishability evidence. Despite those later metadata facts, the checked-in selected-run package for `25841274346` remains the retained non-terminal/provisional snapshot unless a separate terminal package is committed and validated.

A follow-up status review queried the post-packet window across the rollup issue/comment records and local review-record comments, including #2893/#2894 and the records containing #125, #268, #552, #1793, #1794, #1795, #1804, #2540, and #2541. It returned no comments by `human`, `syifan`, or `yifan` after `2026-05-14T20:39:00` and no target issue closures by those authors. Later status reviews repeated the check after the `2026-05-15T09:05Z` planning notes and after the `2026-05-15T23:11:46Z` clean-evidence planning window; they again found no comments, issue updates, closures, or local review-record updates by those authors. The latest status review repeated that check after the missed-deadline planning update at `2026-05-17T13:25:46Z`; issue comments, review-record comments, issue updates/closures, and review-record updates by `human`, `syifan`, or `yifan` were again empty. A further run #440 boundary review found no actual real-human **ACCEPT**/**DECLINE** or **CLOSE**/**KEEP OPEN** decisions after the later completed/failure evidence. Therefore there were no explicit human **ACCEPT**/**DECLINE** responses for #2894, #125, #552, #1793, #1794, #2540, or #2541, and no human **CLOSE**/**KEEP OPEN** responses to the still-pending **CLOSE REQUEST** rows for #268 or #1804. The referenced acceptance/scope items and the #268/#1804 closure requests therefore remain undecided/open at that pre-close checkpoint, while the later #1795 human **CLOSE** decision is carried forward in the rollup and decision packet below. This status update makes no final project-completion claim.

## Historical accuracy/report provenance

The current accuracy tables and historical per-size finishability tables in `accuracy_report.md` are sourced from selected run `25619929396`, not from retained stale finishability run `25710089668`.

| Item | Value |
| --- | --- |
| Run URL | https://github.com/sarchlab/mgpusim-dev/actions/runs/25619929396 |
| Head | `main` / `a281789a7354b86a8a04b350c145c573d531f54d` |
| Workflow | `MI300A Benchmark` |
| Status/conclusion | `completed` / `failure` |
| Accuracy source | `benchmark-comparison/selected-run-25619929396/comparison_ci.detailed.csv` rows with both `real_ms` and `sim_ms` populated |
| Report artifacts | `benchmark-comparison/selected-run-25619929396/validation_report.md` and related selected-run files |

Use the selected-run files for the historical accuracy/report numbers. Use run `25710089668` only for the separate retained stale artifacts-only finishability/provenance evidence above; the latest read-only refresh still found it completed/failure partial diagnostic evidence, not clean completion. Use the checked-in run `25841274346` package only as the retained non-terminal stale provenance snapshot above; the latest refreshed Actions artifacts for that run were completed/failure partial diagnostic evidence, not a clean terminal package. Use run `25886484910` only as stale completed/failure final-artifact evidence with final Summary/final report artifacts, not completed/success current-main evidence and not active/non-terminal evidence. Use run `25910086960` only as stale completed/failure final-artifact evidence with final Summary/final report artifacts, not completed/success current-main evidence and not active/non-terminal/provisional evidence. Use run `26213004542` only as the latest current-main terminal completed/failure final-artifact evidence with successful final Summary upload and final `benchmark-comparison`, `comparison-detailed`, `validation-report`, and `accuracy-report` artifacts; it matches current `main` / `f42286c5e9de738f94b80733ebb0f0cea6123210`, but it is not completed/success clean evidence and `complete_regular_evidence=false` is recorded. Use run `26146663871` only as older same-head completed/failure final-artifact evidence with successful final Summary upload and required final artifacts present; it is stale relative to run `26213004542` at current `main` / `f42286c5e9de738f94b80733ebb0f0cea6123210`, is not current latest evidence, is not completed/success clean evidence, and its final Summary log records upstream benchmark-tier failures with 629/1416 simulation rows and 511/1416 regression rows. Use run `26083372148` only as older completed/failure final-artifact evidence with successful final Summary upload and final `benchmark-comparison`, `comparison-detailed`, `validation-report`, and `accuracy-report` artifacts; it is stale relative to #439/#440 at current `main` / `f42286c5e9de738f94b80733ebb0f0cea6123210`, is not current latest evidence, is not completed/success clean evidence, and no completed/success `complete_regular_evidence` is recorded. Use run `25993763757` only as older completed/failure final-artifact evidence with successful final Summary upload and final `benchmark-comparison`, `comparison-detailed`, `validation-report`, and `accuracy-report` artifacts; it is stale relative to current `main` / `f42286c5e9de738f94b80733ebb0f0cea6123210`, is not current latest evidence, is not completed/success clean evidence, and its final Summary log records partial diagnostic regular evidence with 618/1416 simulation rows and 500/1416 regression rows. Use stale run `25974165191` only as the previous current-main run attempt from `b78207bc1210e0ed2aa6f9b6c9f59a4511b92f35`; its earlier checked state was active/provisional with `validation-summary` plus partial `benchmark-results-*` artifacts and no final Summary/report artifacts, and it must not be reused as clean evidence. Use run `25962281031` only as the previous queued/provisional stale fresh run attempt with `validation-summary` plus partial `benchmark-results-*` artifacts and no final Summary/report artifacts; it is stale relative to current `main` / `f42286c5e9de738f94b80733ebb0f0cea6123210` and is not completed/success current-main evidence. Use run `25946016596` only as the recorded package-attributed earlier then-current-SHA launch and run `25946106740` only as the observed additional earlier then-current-SHA run with unknown/external provenance; those earlier runs are stale relative to `f42286c5e9de738f94b80733ebb0f0cea6123210` and remain non-terminal/failed-partial/provisional attempts unless a future completed/success current-main run with final Summary/report artifacts and `complete_regular_evidence` is collected and validated.

## Benchmark acceptance topic rollup

This rollup summarizes the benchmark-acceptance topics that still need careful status boundaries. It is not an acceptance decision and does not supersede the caveats below.

| Ref | Current status boundary |
| --- | --- |
| #125 | Tier-1 calibration is partial. Selected-run report evidence exists, but memory, compute, and L1/L2 caveats remain. |
| #268 | Tier-1 benchmark analysis and `scripts/run_tier1_hardware.sh` exist. Candidate future probes are identified, with no new hardware collection in this package. |
| #552 | `adjust_weights` uses report-time affine calibration with `sim_scale=0.570694861247` and `fixed_time_ms=0.005321986901` on 11 completed selected-run samples. Raw `sim_ms` rows remain unchanged. Verification semantics were fixed in commit `eb093e13`: when `SkipLayerforward` skips the layerforward kernel, hidden-output verification is skipped but adjusted weights remain verified; `amd/benchmarks/rodinia/backprop/benchmark_test.go` includes the adjusted-weight mismatch regression test. |
| #1793 | BFS uses report-time calibration with scale `133.490552877` and fixed time `0`. The calibrated evidence covers 12 completed samples with 15.89% mean error and 34.88% max error; 6 larger sizes are missing. |
| #1794 | L1/L2 cache-bandwidth evidence remains diagnostic/insufficient: current tracked rows are all hardware-flat (L1 16 HW/16 sim, max/min 1.148×, raw 93.96%; L2 16 HW/5 sim, max/min 1.009×, raw 89.47%). Fixed-offset boosters are diagnostic only; value-changing evidence still requires non-flat loaded rows. |
| #1795 | A real human closed the increased-time workflow-contract request at `2026-05-18T21:06:48.384Z` (`closed_by=human`). The maintained workflow contract remains 7200 seconds per simulator invocation, `timeout-minutes: 360` / 21600 seconds per benchmark-tier job, `strategy.max-parallel: 14`, with Tier 1 before Tier 2. Run `25841274346` is retained only as a non-terminal stale-run provenance snapshot, not terminal finishability evidence. The stale deleted workflow API record for `.github/workflows/benchmark-tier-runner.yml` (workflow id `260768869`) is documented in `docs/github-actions-workflow-hygiene.md` as disabled manually on 2026-05-13, absent from tracked `main`, and benign only while no new runs appear. This human CLOSE is carried forward only for #1795; it does not accept partial finishability, resolve #2894/#2893, or turn failed/non-terminal runs into clean completion evidence. |
| #1804 | The proposed replacement specification is `docs/proposed_spec.md`. The project-root `spec.md` is not edited by this package. |
| #2540 | Acceptance scope remains undecided for whether calibrated `adjust_weights` reporting is sufficient or raw `adjust_weights` accuracy must be resolved. |
| #2541 | Acceptance scope remains undecided for whether diagnostic-only L1/L2 cache-bandwidth rows are sufficient or value-changing cache-bandwidth evidence is required. |

## Final human decision packet

This packet is the human-actionable decision surface for the remaining acceptance rollup. It records no acceptance by itself: no row is preselected, no human-owned issue is closed here by this package, no repository author self-accepts a caveat, and no final project-completion claim is made. A human can choose exactly one option per remaining row and then update the referenced issue(s). The only carried-forward post-planning human decision is the separate #1795 `CLOSE`: issue #1795 (`Increase simulation time`, local issue 361) was closed by `human` at `2026-05-18T21:06:48.384Z`; that closes only the workflow-contract request and does not accept partial finishability or resolve #2894/#2893.

Evidence boundaries for every remaining row: run `25710089668` is only completed/failure partial artifacts-only finishability evidence; the checked-in run `25841274346` package remains non-terminal/provisional and stale relative to the later checked `main` tip unless a separate terminal package is committed and validated; run `26213004542` is the latest current-main terminal completed/failure final-artifact evidence but not completed/success clean evidence and has `complete_regular_evidence=false`; run `26146663871` is older same-head completed/failure final-artifact evidence stale relative to #440; run `26083372148` is older completed/failure final-artifact evidence stale relative to #439/#440; historical accuracy tables remain selected-run `25619929396` evidence.

| Ref | Exact human choice requested | Evidence boundary | If selected |
| --- | --- | --- | --- |
| #2894 | **ACCEPT** partial artifacts-only finishability, or **DECLINE** and require clean terminal maintained-run evidence. | No clean terminal maintained MI300A run is recorded. Run `26213004542` is the latest current-main completed/failure final-artifact evidence, not completed/success clean evidence; run `26146663871` is older same-head completed/failure final-artifact evidence; run `25710089668` is completed/failure partial artifacts-only evidence; run `25841274346` is non-terminal/provisional snapshot evidence only. | **ACCEPT** means partial artifacts-only finishability is acceptable for this blocker without calling it a clean run. **DECLINE** keeps the blocker open until a clean terminal maintained MI300A Benchmark run is collected and validated. |
| #125 | **ACCEPT** current partial Tier-1 calibration with documented caveats, or **DECLINE** and require more Tier-1 calibration/architecture work. | Selected-run report evidence exists, but memory, compute, and L1/L2 caveats remain; retained run `25710089668` is separate stale finishability evidence, not a new accuracy-report source. | **ACCEPT** means the partial Tier-1 calibration is accepted as-is. **DECLINE** keeps calibration acceptance open with explicit new pass criteria or architecture/modeling work. |
| #552 | **ACCEPT** calibrated `adjust_weights` reporting plus the completed verification-semantics fix, or **DECLINE** and require raw simulator accuracy/new measurements. | Commit `eb093e13` keeps adjusted-weight verification active when hidden-output verification is skipped; report-time affine calibration is fitted to 11 completed selected-run samples, while raw `sim_ms` rows remain unchanged. | **ACCEPT** means calibrated reporting and the verification fix are sufficient for this request. **DECLINE** keeps the request open until raw timing accuracy or new measurements satisfy the human criteria. |
| #1793 | **ACCEPT** calibrated partial BFS evidence, or **DECLINE** and require additional BFS tuning/collection for missing larger sizes. | BFS report-time calibration covers 12 completed selected-run samples with 15.89% mean error and 34.88% max error; 6 larger sizes are missing. | **ACCEPT** means the calibrated partial BFS evidence is sufficient. **DECLINE** keeps BFS acceptance open for more data or tuning. |
| #1794 | **ACCEPT** diagnostic-only L1/L2 cache-bandwidth evidence, or **DECLINE** and require value-changing loaded hardware/simulator rows above the >2× gate. | Current tracked L1/L2 rows are hardware-flat diagnostic evidence only: L1 16 HW/16 sim with max/min 1.148×, L2 16 HW/5 sim with max/min 1.009×; fixed-offset boosters remain diagnostic. | **ACCEPT** means diagnostic-only L1/L2 evidence is sufficient despite the caveat. **DECLINE** keeps acceptance open until non-flat loaded rows and matching simulator evidence are collected. |
| #2540 | **ACCEPT** calibrated `adjust_weights` acceptance scope, or **DECLINE** and require raw `adjust_weights` accuracy resolution. | The retained evidence supports calibrated report-time `adjust_weights` reporting and completed verification semantics, not changed raw `sim_ms` accuracy. | **ACCEPT** resolves the scope question in favor of calibrated reporting. **DECLINE** requires a raw simulator accuracy fix or new raw measurements before acceptance. |
| #2541 | **ACCEPT** diagnostic-only L1/L2 acceptance scope, or **DECLINE** and require value-changing cache-bandwidth evidence. | Retained L1/L2 rows are diagnostic/hardware-flat, not loaded non-flat value-changing cache-bandwidth evidence. | **ACCEPT** resolves the scope question in favor of diagnostic-only rows. **DECLINE** requires value-changing loaded rows before acceptance. |
| #268 | **CLOSE REQUEST**: close as technically resolved, or **KEEP OPEN** and request specific new hardware probes. | Tier-1 benchmark analysis and `scripts/run_tier1_hardware.sh` are present, and candidate future probes are identified; no new hardware collection is included here. | **CLOSE REQUEST** asks the human to close the benchmark-analysis request as satisfied. **KEEP OPEN** should name the additional probe collection required. |
| #1804 | **CLOSE REQUEST**: close the suggested-spec request, or **KEEP OPEN** and request adoption or edits. | `docs/proposed_spec.md` is the proposed replacement specification; the human-owned project-root `spec.md` is not edited by this package. | **CLOSE REQUEST** asks the human to close the suggested-spec request as satisfied by the tracked proposal. **KEEP OPEN** should say whether to adopt it into project-root `spec.md` or request edits. |

A later status review after the post-merge planning window, the later recheck after the missed-deadline planning update, and the run #440 boundary review found no qualifying real-human **ACCEPT**/**DECLINE** decision by `human`, `syifan`, or `yifan`. The latest decision recheck after the planning boundary carries forward exactly one qualifying real-human **CLOSE** decision: #1795 was closed by `human` at `2026-05-18T21:06:48.384Z`. No other real-human **CLOSE**/**KEEP OPEN** decision for #268 or #1804 was found, and #2894 and #2893 remain open/unresolved absent completed/success `complete_regular_evidence` or actual real-human decisions. After additional human choices are recorded, update the acceptance rollup without reclassifying partial/failing/non-terminal CI as clean completion. Until then, #2894 and the human-only acceptance/scope rows remain undecided, and the remaining closure-request rows remain requests rather than issue closures.

## Remaining caveats

- `adjust_weights` has a report-time affine calibration (`sim_scale=0.570694861247`, `fixed_time_ms=0.005321986901`) fitted to 11 completed selected-run samples. The retained current-main recompute reports 59.29% raw mean error and 32.10% configured fixed-time-only mean error; the best non-negative fixed-time-only lower bound is 25.60%, blocked by small-size underprediction and large-size overprediction. The raw `sim_ms` rows remain unchanged, so the calibrated report line should not be read as resolving raw `adjust_weights` accuracy. This raw-accuracy caveat is separate from commit `eb093e13`: `SkipLayerforward` now skips hidden-output verification but still validates adjusted weights, and `benchmark_test.go` covers an adjusted-weight mismatch.
- L1/L2 cache-bandwidth rows remain provenance-sensitive. Archived selected-run fixed-work cache-residency rows are diagnostic only when their provenance gate passes. The current tracked rows are all hardware-flat (L1 16 hardware / 16 completed sim rows, hardware max/min 1.148×; L2 16 hardware / 5 completed sim rows, hardware max/min 1.009×), so fixed-offset boosters and trend/shape metrics remain diagnostic. Current work-scaling rows should count as value-changing evidence only after loaded rows demonstrate non-flat hardware behavior above the >2× value-change gate.
- The status above preserves the separation between maintained workflow contract, the latest current-main terminal failed final-artifact evidence, historical selected-run accuracy reporting, retained stale terminal artifacts-only finishability evidence, and the retained non-terminal stale-run provenance snapshot.
