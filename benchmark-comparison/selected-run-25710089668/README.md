# Selected MI300A Benchmark run 25710089668 artifacts

Tracked copy of terminal-run artifacts collected to bind the
current-main MI300A regular workflow evidence to dispatched run `25710089668`.

Source: curated selected artifacts from MI300A Benchmark run
`25710089668` on `main` head `6fdbbd1882f6a36c5e0846b9209a9a19c258b486`.
Run status/conclusion: `completed` / `failure`; the final Summary job succeeded
and uploaded the report artifacts. That Summary success is artifact/report upload
completion only, not a clean benchmark completion claim and not final project
completion evidence.

These files preserve the detailed comparison, merged simulation, regression,
regular artifact coverage, validation-summary contract artifacts, validation
report byte manifest, run/job metadata, and artifacts-only finishability evidence
selected for terminal current-main provenance.

## Run and artifact inventory

- Workflow/event: `MI300A Benchmark` / `workflow_dispatch`
- Head branch/SHA: `main` / `6fdbbd1882f6a36c5e0846b9209a9a19c258b486`
- Run URL: https://github.com/sarchlab/mgpusim-dev/actions/runs/25710089668
- Run metadata in `run-view.json`; job inventory records in `jobs.json`: `85` jobs.
- GitHub Actions artifact inventory records in `artifacts.tsv`: `87` artifact inventory rows.
- Raw artifact ZIP file entries in `raw-zip-entry-counts.tsv`: `340` raw ZIP file entries across those 87 artifact inventory rows; raw ZIP SHA256s are recorded in `raw-zip-sha256.tsv`.
- This checked-in directory is a curated retained subset of selected run metadata, manifests, reports, summary CSVs, and finishability evidence; it is not a full extraction of all raw artifact ZIP entries.

## Final artifact identities

| Artifact | ID | Raw zip SHA256 |
| --- | ---: | --- |
| `accuracy-report` | 6958363778 | `e7b683211e7068432af3ca9847ab7c25345de7530732fcb92ffc59503e996dd1` |
| `validation-report` | 6958363542 | `b71c6b8f0396a50b109b094fd304d8db87094d29afa4cceecd51d3e53243b475` |
| `comparison-detailed` | 6958362089 | `0608413116c4ed5b0537c9b376965f6be62c7e7a2fc443b9189e938e2c472313` |
| `benchmark-comparison` | 6958361983 | `8475e12b555c0d53cb9f494f8a8cf7f4ca637210bfb475a4c3b8f47aa67fa21b` |
| `validation-summary` | 6934525649 | `5117d45d4314cf6e9f9b9571eace3c1417b4d8257b8f713cd3939d72952fb04d` |


`comparison_ci.detailed.csv` is the extracted `comparison-detailed` artifact
CSV. Its SHA256 is `80031f376986ae24edf9cf18ee189ce4fe23570b98b1c9343db8c74b707167db`.

## Derived adjust_weights evidence

`adjust_weights_recomputed_evidence.json` and `.md` are deterministic derived
summaries generated from the tracked `comparison_ci.detailed.csv` and
`scripts/benchmark_tiers.json`. They report raw, fixed-time-only, current
affine, and fixed-time-only lower-bound `adjust_weights` metrics without
rewriting the extracted comparison CSV. The feasibility decision records raw
mean error at 59.29%, configured fixed-time-only mean error at 32.10%, and the
best non-negative fixed-time-only lower bound at 25.60%, so the retained
evidence keeps the affine line as report-time calibration and leaves raw
`sim_ms` rows unchanged.

## Validation-report figure byte provenance

The selected validation-report artifact is `validation-report` artifact
`6958363542` from the same run/head. The raw artifact zip digest is recorded
in `raw-zip-sha256.tsv`; `validation-report-6958363542-sha256.tsv` records extracted validation-report file
hashes.
- `docs/figures/sim_vs_hw_scatter.png` sha256 `7b6f3011ba800d75680673a6d88550213b5920a1d4ac2f9a874834c825add345`.
- `docs/figures/slowdown_bar_chart.png` sha256 `ce24d0b08e44699a2e5bf57a6007782d35f7d1049baa8be9c0c4d1b1103fff2e`.


## Maintained workflow contract verification

The fetched `validation-summary/regular_workflow_contract_validation.json` and
`regular_matrix_validation.json` both report `status: pass`. They validate the
maintained regular contract from `.github/workflows/benchmark.yml`:

- 82 benchmark matrix rows: 19 Tier 1 and 63 Tier 2.
- 1416 plan entries: 308 Tier 1 and 1108 Tier 2.
- Per-simulator invocation timeout: 7200 seconds.
- Per benchmark-tier job budget: `timeout-minutes: 360` / 21600 seconds.
- `strategy.max-parallel: 14` for both benchmark tiers.
- Linear ordering: Tier 1 runs before Tier 2.

## Artifacts-only finishability evidence

`mi300a_regular_finishability_evidence.json` and `.md` are regenerated by the
maintained offline deriver from the checked-in selected-run package: run/job
metadata, the regular matrix report, the regular plan, and the curated merged
`sim_results_ci.csv` timing rows. Missing planned rows are classified as
`no-result`, not as timeout or crash, because the derivation intentionally does
not infer
per-size outcomes from absent log text or job-level failure alone.

- Merged simulator CSV rows: `632`
- Detailed comparison rows: `1416`
- Regression rows: `515`
- Regular matrix rows / plan entries: `82` / `1416`
- Finishability status counts: pass=623, no-result=793, fail=0, timeout=0, cancelled=0
- Benchmark job conclusions: cancelled=4, failure=76, success=2

## Historical selected-run note

This directory does not rewrite the older selected-run archive. It records
terminal evidence for the current-main run. Existing reports that explicitly
cite `benchmark-comparison/selected-run-25619929396/` remain historical selected-run
provenance until a separate report-selection change replaces them.
