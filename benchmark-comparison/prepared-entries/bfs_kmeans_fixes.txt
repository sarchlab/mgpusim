# =============================================================================
# Corrected benchmark.yml entries for bfs and kmeans
# =============================================================================
# These replace the existing entries in benchmark.yml.
# Both benchmarks had incomplete sizes and kmeans had wrong size_label_template.
#
# The fix converts from compound format (node_degree, points_features)
# to single-param format with fixed secondary parameters, matching HW
# reference data which uses just the primary parameter as problem_size.
# =============================================================================

          # -----------------------------------------------------------------
          # bfs: -node N -degree 8 -depth 100
          # HW kernel_name: bfs, problem_size: N
          # ALL 18 reference sizes (fixed from 5)
          # Using fixed degree=8 since HW data only varies node count
          # -----------------------------------------------------------------
          - benchmark: bfs
            sizes: "1024 2048 4096 8192 16384 32768 65536 131072 196608 262144 524288 786432 1048576 1572864 2097152 2621440 3145728 4194304"
            flag_template: "-node {size} -degree 8 -depth 100"
            size_label_template: "{size}"
            timeout_sec: 10800

          # -----------------------------------------------------------------
          # kmeans: -points N -features 32 -clusters 5 -max-iter 1
          # HW kernel_name: kmeans, problem_size: N
          # ALL 16 reference sizes (fixed from 5)
          # Using fixed features=32 since HW data only varies point count
          # Fixed size_label_template from "{p1}_f{p2}_i1" to "{size}"
          # -----------------------------------------------------------------
          - benchmark: kmeans
            sizes: "1024 2048 4096 8192 16384 32768 65536 131072 262144 524288 786432 1048576 1572864 2097152 3145728 4194304"
            flag_template: "-points {size} -features 32 -clusters 5 -max-iter 1"
            size_label_template: "{size}"
