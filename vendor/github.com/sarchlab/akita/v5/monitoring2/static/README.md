# Monitoring2 Static

React/shadcn frontend for Akita's live in-situ monitor.

Run locally against a simulation on the default monitor port:

```sh
npm run dev
```

Use another monitor target:

```sh
AKITA_MONITOR_URL=http://localhost:32777 npm run dev
```
